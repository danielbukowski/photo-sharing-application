spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/app_db
    username: postgres
    password: password
    driverClassName: org.postgresql.Driver

